import 'package:flutter/material.dart';

class HalamanDocument extends StatelessWidget {
  const HalamanDocument({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('Documen Page'),
    ));
  }
}
