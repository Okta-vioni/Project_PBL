import 'package:flutter/material.dart';

class HalamanAccounts extends StatelessWidget {
  const HalamanAccounts({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('Account Page'),
    ));
  }
}
