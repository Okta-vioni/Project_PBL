import 'package:flutter/material.dart';

class HalamanLocate extends StatelessWidget {
  const HalamanLocate({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('Locate Page'),
    ));
  }
}
